
box: fedora
build:
  steps:
    - install-packages:
        packages: make golang glide nodejs npm
    - glide-install
    - npm-install
    - nahody/jspm-install:
        github-auth: $JSPM_INSTALL_GITHUB_AUTH
    - script:
        name: make
        code: make
